export function OwlIcon({ className = 'w-5 h-5' }: { className?: string }) {
  return (
    <span className={`font-serif font-bold italic select-none ${className}`} style={{ lineHeight: 1 }}>
      L
    </span>
  )
}
